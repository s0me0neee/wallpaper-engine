[project]
name = "wallpaper-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "s0me0ne", email = "mym081230@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pyunpack>=0.3",
]

[project.scripts]
wallpaper-engine = "wallpaper_engine:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
