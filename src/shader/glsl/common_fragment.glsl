// Fragment-side helpers.
//
// Nothing in the sample corpus calls anything from here that common.h does not
// already provide. Kept so the #include resolves rather than failing, and as
// the place to put fragment-only helpers when a wallpaper turns out to need
// one.

#ifndef WE_COMMON_FRAGMENT_H
#define WE_COMMON_FRAGMENT_H
#endif
